import React from "react";

const Button = () => {
  return (
    <button className="btn w-full rounded-full  btn-primary text-center">
      Book Appointment Now
    </button>
  );
};

export default Button;
